import React, {Component} from 'react';
import AppContainer from './Navigator';

/**
 * Main class of the app, renders the navigator component.
 * @class App
 * @extends {Component}
 */
export default class App extends Component<Props> {
  render() {
    return <AppContainer />
  }
}
